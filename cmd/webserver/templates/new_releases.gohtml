{{define "new_releases"}}
    {{ template "_header" . }}

    <div class="container" id="new-releases-page">

        <div class="jumbotron">
            <h1><i class="fas fa-hourglass-end"></i> New Releases</h1>
        </div>

        {{ template "_flashes" . }}

        <div class="card">
            {{ template "_apps_header" . }}
            <div class="card-body">

                <div class="row">
                    <div class="d-none">
                        {{/* This div is so Chrome auto-fills this one and not the rest! */}}
                        <label for="hidden">Hidden</label>
                        <select multiple data-placeholder="Hidden" class="form-control form-control-chosen" id="hidden" name="hidden">
                        </select>
                    </div>
                    <div class="col-sm-6 col-lg-4">
                        <div class="form-group">
                            <label for="days">Number of days to look back</label>
                            <input class="form-control" type="number" id="days" name="days" value="{{ .Days }}" min="1" max="30">
                        </div>
                    </div>
                    <div class="col-sm-6 col-lg-4">
                        <div class="form-group">
                            <label for="search">Search for a Game</label>
                            <input class="form-control" type="search" placeholder="Game Name" id="search" name="search" autofocus>
                        </div>
                    </div>
                </div>

                <div class="table-responsive">
                    <table class="table table-hover table-striped table-counts mb-0" data-path="/games/new-releases/new-releases.json">
                        <thead class="thead-light">
                        <tr>
                            <th scope="col">App</th>
                            <th scope="col">Price</th>
                            <th scope="col">Score</th>
                            <th scope="col">Players</th>
                            <th scope="col" nowrap="nowrap">Release Date</th>
                            <th scope="col">Trend Line</th>
                        </tr>
                        </thead>
                        <tbody>

                        </tbody>
                    </table>
                </div>

            </div>
        </div>

        <div class="jumbotron bottom mt-3">
            <h1><i class="fas fa-info"></i></h1>
            <div>We are only able to show apps that have their release date in a format we can read, so there may be some missing.</div>
        </div>

    </div>
    {{ template "_footer" . }}
{{end}}
