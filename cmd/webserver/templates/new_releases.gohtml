{{define "new_releases"}}
    {{ template "_header" . }}

    <div class="container" id="new-releases-page">

        <div class="jumbotron">
            <div class="row">
                <div class="col-sm-12 col-lg-6">

                    <h1><i class="fas fa-hourglass-end"></i> New Releases</h1>

                </div>
                <div class="col-sm-12 col-lg-6">

                    <div class="input-group input-group-lg mt-1 mb-2">
                        <input class="form-control" type="search" name="search" placeholder="Search for a Game" id="search" autofocus>
                        <div class="input-group-append">
                            <input type="submit" value="Search" class="input-group-text">
                        </div>
                    </div>

                </div>
                <div class="col-12">
                    <p class="lead">The {{ comma64 .Apps }} apps released in the last {{ comma .Days }} days.</p>
                </div>

            </div>
        </div>

        {{ template "_flashes" . }}

        <div class="card">
            {{ template "_apps_header" . }}
            <div class="card-body">

                <div class="table-responsive">
                    <table class="table table-hover table-striped mb-0" data-path="/new-releases/new-releases.json">
                        <thead class="thead-light">
                        <tr>
                            <th scope="col">App</th>
                            <th scope="col">Price</th>
                            <th scope="col">Score</th>
                            <th scope="col">Players</th>
                            <th scope="col" nowrap="nowrap">Release Date</th>
                            <th scope="col">Trend Line</th>
                        </tr>
                        </thead>
                        <tbody>

                        </tbody>
                    </table>
                </div>

            </div>
        </div>

        <div class="jumbotron bottom mt-3">
            <h1><i class="fas fa-info"></i></h1>
            <div>We are only able to show apps that have their release date in a format we can read, so there may be some missing.</div>
        </div>

    </div>
    {{ template "_footer" . }}
{{end}}
