{{define "_apps_header"}}

    <div class="card-header">

        <ul class="nav nav-tabs card-header-tabs" role="tablist">

            <li class="nav-item">
                {{if startsWith .Path "/login" }}
                    <span class="nav-link active" role="tab">Login</span>
                {{else}}
                    <a class="nav-link" href="/login" role="tab">Login</a>
                {{end}}
            </li>

            <li class="nav-item">
                {{if startsWith .Path "/signup" }}
                    <span class="nav-link active" role="tab">Signup</span>
                {{else}}
                    <a class="nav-link" href="/signup" role="tab">Signup</a>
                {{end}}
            </li>

        </ul>
    </div>

{{end}}
